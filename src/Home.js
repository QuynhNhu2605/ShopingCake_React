import React from "react";
import { Navbar, Nav } from "react-bootstrap";

const Home = ({ menus, setCategory }) => {
    return (
        <div>
            Welcome
        </div>
    );
};

export default Home;
